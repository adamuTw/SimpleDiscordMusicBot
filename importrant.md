既然你希望能有更深度的複習資料，我將這六份作業的核心知識點拆解成**「手把手教學指南」**。這份指南不只給你答案，而是教你「如何思考」與「避開陷阱」。

---

## 第一章：數位資料的精確計算 (HW 1 & 2)

### 1. 二補數 (Two's Complement) 的極致細節

**為什麼要用二補數？** 為了讓電腦可以用「加法器」來做「減法」。

* **正數轉負數（二補數）的快捷鍵：**
1. 從二進位的**最右邊**開始往左找。
2. 在遇到第一個 `1` 之前，所有的 `0` 都不變。
3. 遇到第一個 `1` 時，那個 `1` 也不變。
4. 之後**左邊的所有位元**全部反轉（0 變 1，1 變 0）。


* *範例：*   第一個 1 在倒數第三位  。


* **溢位判斷 (Overflow)：**
* 在 -bit 運算中，若「兩個正數相加得到負數」或「兩個負數相加得到正數」，則發生溢位。
* **注意：** 一正一負相加絕對不會溢位。



### 2. IEEE 754 浮點數轉換：5 步標準化

題目：將  轉為 32-bit IEEE 格式。

1. **整數與小數分離：** ；（連續乘 2 取整數）。
2. **合併與正規化：** 。
3. **符號位 (Sign)：** 正數  `0`。
4. **指數位 (Exponent)：** 指數是 ，加上 Bias 。將  轉二進位  `10000101`。
5. **尾數位 (Mantissa)：** 取正規化後小數點後的數字 `111011101001`，後面補 `0` 直到滿 23 位。

---

## 第二章：電腦組成架構 (HW 3)

### 1. 記憶體與位址線的關係

這是最常考的邏輯題。記住公式：


* **變形題：** 若記憶體從 16 GB 增加到 64 GB，定址位元需要增加幾位？
* *解析：* ，所以需要多增加 **2 bits**。



### 2. 指令格式設計 (Instruction Format)

若一個指令格式為 `add M R1 R2`，要計算其最小 bit 數：

* **Opcode：** 16 種指令 ，需要 **4 bits**。
* **Registers：** 8 個暫存器 ，每個暫存器欄位需要 **3 bits**。
* **Memory Address (M)：** 根據記憶體單元數決定。4096 個單元 ，需要 **12 bits**。
* **總長度：** 。

---

## 第三章：網路層級與傳輸 (HW 4)

### 1. TCP/IP 協定疊層任務

* **實體層 (Physical)：** 將位元 (Bits) 轉為電磁訊號。
* **資料鏈結層 (Data Link)：** 處理相鄰節點間的幀 (Frames) 傳輸、錯誤偵測。
* **網路層 (Network)：** 路由選擇 (Routing)、路徑決定、IP 定址。
* **傳輸層 (Transport)：** 提供端對端 (End-to-End) 的可靠傳輸 (TCP) 或快速傳輸 (UDP)。
* **應用層 (Application)：** 直接為使用者提供服務 (HTTP, FTP, DNS)。

---

## 第四章：演算法與資料結構 (HW 5 & 6)

### 1. 樹的重建 (Tree Reconstruction)

**考點：** 給予 In-order (ABDC) 與 Post-order (DBAC)，求原樹。

* **邏輯：**
1. Post-order 的最後一個字元一定是 **Root**。
2. 在 In-order 中找到 Root，其左邊的所有字元屬於 **左子樹**，右邊屬於 **右子樹**。
3. 不斷重複此過程。


* **陷阱：** 如果 In-order 與 Post-order 的字元集合不對應，或者邏輯衝突，則該樹不存在。

### 2. 分數加法演算法 (Fraction Addition)

這題考的是如何處理「最大公因數 (GCD)」。

1. **通分：** $\frac{a}{b} + \frac{c}{d} = \frac{ad + bc}{bd}$ 。
2. **求 GCD：** 使用輾轉相除法求出  與  的最大公因數。
3. **約分：** 分子分母同時除以 GCD。

---

## 第五章：資料庫 SQL 查詢 (DB HW)

### 1. 連表查詢 (JOIN) 的核心

當你要查詢的資料（如：員工姓名）在 A 表，但篩選條件（如：公司城市）在 B 表時：

* **標準寫法：**
```sql
SELECT E.employee_name
FROM employee E, works W, company C
WHERE E.employee_name = W.employee_name 
  AND W.company_name = C.company_name
  AND C.city = 'Kaohsiung' 
  AND E.city <> 'Kaohsiung';

```


* **重點：** 必須設定「關聯鍵相等」()，否則會產生笛卡兒積（錯誤的大量資料）。

---

## 🎯 期末考可能的「進階變形題」

1. **浮點數加法：** 兩個 IEEE 754 格式的數相加，步驟為何？（提示：先對齊指數，再相加尾數）。
2. **記憶體對齊：** 如果 Word Size 是 4 Bytes，但一個資料只佔 1 Byte，在記憶體中如何存放？
3. **樹的走訪：** 如果一棵樹只有左子樹，它的 Pre-order 和 In-order 會有什麼關係？（提示：In-order 會是 Pre-order 的反轉）。
4. **SQL 聚合：** 如何計算每間公司的平均薪資？(`SELECT company_name, AVG(salary) FROM works GROUP BY company_name`)

**這份補充的詳細程度符合你的需求嗎？如果你想針對「演算法的虛擬碼撰寫」或「特定的 SQL 複雜查詢」進行練習，請告訴我！**
