第一單元：數位資料表示法（精通計算細節）1. IEEE 754 浮點數：從十進位到 32-bit這是最容易在小細節出錯的地方。請記住這三個組成：符號 (S)、指數 (E)、尾數 (M)。關鍵步驟：二進位化：將整數與小數分開轉。例如 $24.75 \rightarrow 11000.11_2$。正規化：移動小數點直到剩下 $1.xxxxx$。移動的位數就是你的指數 $n$。例：$1.100011 \times 2^4$（指數 $n=4$）。計算指數 (E)：$E = n + 127$。將此結果轉為 8 位元二進位。填入尾數 (M)：取小數點後的數字，由左往右填入 23 位元，空格補 0。常見陷阱：轉換回十進位時，別忘了公式是 $(-1)^S \times (1 + M) \times 2^{E-127}$。那個「$1+$」是關鍵。2. 二補數與溢位 (Overflow)快速求補數：從二進位最右邊往左看，遇到第一個 "1" 之前的所有 0 不變，第一個 "1" 也不變，之後左邊的所有位元全部反轉（0 變 1，1 變 0）。溢位判斷：只有「同號相加」才可能溢位。如果兩個正數相加結果變成負數（最左位元為 1），就是溢位。第二單元：電腦架構與硬體（理解硬體邏輯）1. 記憶體定址計算（作業 3 重點）這類題目考的是「空間與位址的數量關係」。公式：$2^{\text{位址位元數}} = \frac{\text{總記憶體容量}}{\text{一個定址單元的大小}}$。複習題練習：若記憶體 64 GB，且電腦是 Byte-addressable（每個位址存 1 Byte），需要幾位元？解：$64\text{ GB} = 2^6 \times 2^{30} = 2^{36}$。答案是 36 bits。2. CPU 內部的暫存器Program Counter (PC)：儲存「下一條」指令的位址。Instruction Register (IR)：儲存「當前正在執行」的指令碼。延伸思考：如果指令長度是 32 bits，則 IR 必須至少是 32 bits。第三單元：演算法與資料結構（邏輯推演）1. 二元樹遍歷 (Binary Tree Traversal)這是必考題。請練習用「手繪路徑法」：前序 (Pre-order)：路徑經過節點的左側時記錄。中序 (In-order)：路徑經過節點的底部時記錄。後序 (Post-order)：路徑經過節點的右側時記錄。重建樹 (HW6-4)：後序最後一個是 Root。在中序中找到該 Root，左邊就是左子樹，右邊就是右子樹。重複此步驟直到填滿。2. 虛擬碼邏輯 (Pseudo-code)回文判斷 (Stack)：Plaintext1. 將字串前半段字元依序 Push 入 Stack。
2. 如果字串長度為奇數，跳過中間那個字元。
3. 從後半段字元開始，每讀一個字元就從 Stack Pop 出一個進行比較。
4. 只要有一次不對稱，就不是回文。
第四單元：網路通訊（傳輸與協定）1. 網路效率計算公式：$\text{效率} = \frac{\text{原始數據大小}}{\text{原始數據} + \text{所有層級的 Header 總和}}$。這反映了為什麼「小封包過多」會導致網路變慢，因為 Header 佔比太高。2. 常用埠號 (Well-known Ports)請務必背下這幾個：80: HTTP443: HTTPS22: SSH25: SMTP (郵件發送)第五單元：資料庫與 SQL（數據管理）1. SQL 語法邏輯連表查詢 (Join)：當題目要求「找出在 A 公司工作的員工城市」時，你需要連接 works 表和 employee 表。語法結構：SQLSELECT 欄位名
FROM 表格 A, 表格 B
WHERE A.關聯鍵 = B.關聯鍵 AND 其他過濾條件;
2. E-R 圖轉關聯表 (Schema)1:N 關係：將「1」那方的主鍵 (Primary Key) 放到「N」那方當作外鍵 (Foreign Key)。M:N 關係：必須產生一張新表，裡面包含兩邊的主鍵。
